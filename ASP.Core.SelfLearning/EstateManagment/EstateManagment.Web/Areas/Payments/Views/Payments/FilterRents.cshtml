@model FilterRentsViewModel
@{
    ViewData["Title"] = "Резултат от филтрирането на недвижимостите и парко местата";
}

<div class="panel panel-default">
    <div class="panel-heading">
        <h2 class="panel-title">@ViewData["Title"]</h2>
    </div>
    <div class="panel-body">
        <div class="col-md-2">

            <div>
                От:<strong> @Model.StartDate.ToShortDateString()</strong>
            </div>
            <div>
                До:<strong> @Model.EndDate.ToShortDateString()</strong>
            </div>
        </div>
        <div class="col-md-3">
            <div>
                Всички приходи:<strong>@Model.TotalIncome лв.</strong> 
            </div>
            <div>
                &nbsp;&nbsp;Чисти приходи:<strong>@((Model.TotalIncome - Model.VAT).ToString("F2")) лв.</strong> 
            </div>
        </div>
        <div class="col-md-3">
            <div>
                от тях: <strong>@Model.PaidInCash лв.</strong> в брой!
            </div>
            <div>
                &nbsp;&nbsp;ДДС: <strong>@Model.VAT.ToString("F2") лв.</strong> за периода!
            </div>
        </div>
        <div class="pull-right">
            <a href="javascript:window.history.back()" class="btn btn-default pull-right">Назад</a>
        </div>
    </div>
    @if (Model.Payments != null && Model.Payments.Count() > 0)
    {
        <div class="row">
            <div class="col-md-12 col-lg-12">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th scope="col">Платено</th>
                            <th scope="col">Платено на</th>
                            <th scope="col">Краен срок</th>
                            <th scope="col">Пълна сума</th>
                            <th scope="col">ДДС</th>
                            <th scope="col">Начин</th>
                            <th scope="col">Отразено от</th>
                            <th scope="col">Клент</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var payment in Model.Payments)
                        {
                            <tr>
                                <td class="@(payment.Amount!=payment.TotalPayment?"text-danger":"")"><strong>@payment.Amount.ToString("F2")</strong></td>
                                <td>@payment.PaidOn.ToShortDateString()</td>
                                <td>@payment.DeadLine.ToShortDateString()</td>
                                <td>@payment.TotalPayment.ToString("F2")</td>
                                <td>@(payment.ApplyVAT == true ? "Включено" : "Без")</td>
                                <td>@(payment.CashPayment == true ? "В брой" : "Банка")</td>
                                <td>@payment.Username</td>
                                <td>
                                    <a asp-area="" asp-controller="Clients" asp-action="DetailsByName" asp-route-name="@payment.Client">@payment.Client</a>
                                </td>
                                <td class="pull-right">
                                    <a asp-area="" asp-controller="Rents" asp-action="Details" asp-route-id="@payment.RentAgreementId" class="btn btn-default">Договор Детайли</a>
                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <h3>Няма записи за плащания на наем в зададения филтър</h3>
    }
</div>
